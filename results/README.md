# Results

This folder contains selected representative results only.

- `reference_runs/`: compact `.npz` files copied from the original runs.
- `reference_figures/`: selected figures suitable for the README, presentation, or quick review.
- `runs/`: generated when running the scripts locally; not included by default.

Avoid uploading every raw restart to GitHub. Keep only the result files that support the summary.
