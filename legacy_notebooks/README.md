# Legacy notebooks

These are the original notebooks kept for traceability. They are not the main entry point anymore.

Use the cleaned modules in `src/floquet_ness_qoc/` and the scripts in `scripts/` for future work.
