[project]
name = "floquet-ness-qoc"
version = "0.1.0"
description = "Floquet nonequilibrium steady-state engineering with Fourier-parameterized quantum optimal control"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "matplotlib>=3.7",
]

[tool.setuptools.packages.find]
where = ["src"]
